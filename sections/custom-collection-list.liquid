{% for mycollections in section.settings.collection_list %}
  {{ mycollections.title }}<br>
  {% endfor %}

<br><br><br>
{% for allcollection in collection %}
  {{ allcollection.title }}<br>
  {% endfor %}

{% schema %}
  {
    "name": "custom-collection-list",
    "settings": [
      {
        "type": "collection_list",
        "id": "collection_list",
        "label": "Select Collections"
      }
    ],
    "presets": [
      {
        "name": "Custom CollectionList"
      }
    ]
  }
{% endschema %}